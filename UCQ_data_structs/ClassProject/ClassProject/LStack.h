#pragma once

#include "List.h"

// if not defined -> si no está definido
#ifndef LSTACK
#define LSTACK


template <typename T>
class LStack
{
	List<T> data; // es la única variable que tiene que tener dentro.


};

#endif // !LSTACK